#productDetailsModal, #cartModal { /* Corrected ID */
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7); /* Dark overlay */
    display: flex; /* To center content */
    align-items: center;
    justify-content: center;
    z-index: 1000; /* Ensure it's on top */
    /* display: none; is handled by inline style in HTML */
}

#productDetailsModal > div { /* Product detail modal content */
    background-color: white;
    padding: 30px;
    border-radius: 10px;
    max-width: 800px; /* Slightly wider modal */
    width: 90%; /* Responsive width */
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.4);
    display: flex; /* Arrange image and text side-by-side */
    flex-wrap: wrap; /* Allow wrapping on small screens */
    gap: 30px; /* More space between image and text */
}

#productDetailsModal img {
    width: 100%; /* Full width on small screens */
    max-width: 350px; /* Max width for image */
    height: auto;
    object-fit: cover;
    border-radius: 8px;
    display: block; /* Ensure it respects width/height */
    margin: 0 auto; /* Center image */
}

#productDetailsModal > div > div:last-of-type { /* Text content for product detail */
    flex: 1; /* Takes remaining space */
    min-width: 280px; /* Minimum width before wrapping */
    text-align: left;
}

#productDetailsModal h3 {
    font-size: 28px;
    margin-top: 0;
    margin-bottom: 12px;
    color: #333;
}

#productDetailsModal p {
    font-size: 16px;
    color: #555;
    margin-bottom: 20px;
}

#productDetailsModal span { /* Price */
    font-size: 24px;
    font-weight: bold;
    color: #28a745; /* Green price */
    display: block; /* Puts price on its own line */
    margin-bottom: 20px;
}

#modalProductOptions {
    margin-bottom: 20px;
}

#modalProductOptions label { /* For size/color labels if added */
    display: block;
    font-weight: bold;
    margin-bottom: 8px;
    color: #444;
}

#modalProductOptions button { /* Size/Color buttons */
    background-color: #eee;
    border: 1px solid #ccc;
    padding: 8px 15px;
    border-radius: 5px;
    cursor: pointer;
    margin-right: 8px;
    margin-bottom: 8px;
    font-size: 14px;
}

#modalProductOptions button:hover,
#modalProductOptions button.selected { /* A simple way to highlight selected */
    background-color: #007BFF;
    color: white;
    border-color: #007BFF;
}

#productDetailsModal > div > div:last-of-type > div:last-of-type { /* Buttons container in modal */
    margin-top: 25px;
    display: flex;
    flex-direction: column;
    gap: 10px;
}

#modalAddToCartBtn {
    background-color: #007BFF;
    color: white;
    border: none;
    padding: 14px 25px;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
    width: 100%; /* Full width button */
}

#modalAddToCartBtn:hover {
    background-color: #0056b3;
}

#productDetailsModal button:last-of-type { /* Close button in product modal */
    background-color: #6c757d; /* Gray close button */
    margin-top: 0; /* Remove extra margin */
}

#productDetailsModal button:last-of-type:hover {
    background-color: #5a6268;
}


/* Cart Modal (Sidebar) */
#cartModal > div { /* Cart modal content */
    background-color: white;
    width: 380px; /* Slightly wider cart */
    height: 100%;
    position: absolute; /* Positions it to the right */
    right: 0;
    top: 0;
    padding: 25px;
    box-shadow: -5px 0 15px rgba(0, 0, 0, 0.25); /* Stronger shadow */
    display: flex;
    flex-direction: column;
    justify-content: space-between; /* Pushes checkout to bottom */
}

#cartModal h3 {
    font-size: 26px;
    margin-top: 0;
    margin-bottom: 25px;
    border-bottom: 1px solid #eee;
    padding-bottom: 10px;
}

#cartModal button:first-of-type { /* Close button in cart header */
    background: none;
    border: none;
    font-size: 28px;
    cursor: pointer;
    position: absolute;
    top: 15px;
    right: 20px;
    color: #555;
}

#cartModal button:first-of-type:hover {
    color: #333;
}

#cartItemsContainer {
    flex-grow: 1; /* Takes available space */
    overflow-y: auto; /* Allows scrolling if many items */
    padding-right: 15px; /* Space for scrollbar */
    margin-bottom: 20px; /* Space before total */
}

#cartItemsContainer > div { /* Individual cart item */
    display: flex;
    align-items: center;
    margin-bottom: 15px;
    border-bottom: 1px dashed #eee;
    padding-bottom: 10px;
    flex-wrap: wrap; /* Allow wrapping */
}

#cartItemsContainer img {
    width: 70px; /* Larger image */
    height: 70px;
    object-fit: cover;
    border-radius: 5px;
    margin-right: 15px;
    flex-shrink: 0; /* Prevent shrinking */
}

#cartItemsContainer h4 {
    margin: 0;
    font-size: 17px;
    color: #444;
    flex-grow: 1; /* Allows text to take space */
    min-width: 150px; /* Prevent it from getting too small */
}

#cartItemsContainer p {
    margin: 0;
    font-size: 14px;
    color: #666;
    width: 100%; /* Ensure size/color info is on new line */
}

#cartItemsContainer input[type="number"] {
    width: 60px; /* Wider quantity input */
    padding: 6px;
    border: 1px solid #ccc;
    border-radius: 3px;
    text-align: center;
    margin-left: auto; /* Pushes to the right */
    margin-right: 10px;
}

#cartItemsContainer button { /* Remove item button */
    background-color: #f44336;
    color: white;
    border: none;
    padding: 6px 10px;
    border-radius: 3px;
    cursor: pointer;
    font-size: 13px;
    position: static; /* Override absolute position if any */
}

#cartItemsContainer button:hover {
    background-color: #d32f2f;
}

#emptyCarMessage { /* Corrected ID from HTML */
    text-align: center;
    color: #999;
    font-style: italic;
    padding: 30px;
}

#cartModal > div > div:last-child { /* Cart total and checkout button */
    padding-top: 20px;
    border-top: 1px solid #eee;
}

#cartModal > div > div:last-child > div:first-child {
    display: flex;
    justify-content: space-between;
    font-size: 19px;
    font-weight: bold;
    margin-bottom: 18px;
    color: #333;
}

#checkoutBtn {
    background-color: #28a745;
    color: white;
    border: none;
    padding: 14px 25px;
    border-radius: 5px;
    font-size: 17px;
    cursor: pointer;
    width: 100%;
}

#checkoutBtn:hover {
    background-color: #218838;
}

#checkoutBtn:disabled {
    background-color: #cccccc;
    cursor: not-allowed;
}

/* --- Message Box --- */
#messageBox {
    position: fixed;
    top: 20px;
    right: 20px;
    background-color: #5cb85c; /* Success green */
    color: white;
    padding: 12px 25px;
    border-radius: 5px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.25);
    z-index: 1001; /* Above modals */
}


/* Responsive adjustments for smaller screens (e.g., mobile) */
@media (max-width: 768px) {
    header > div {
        flex-direction: column; /* Stack header items vertically */
        gap: 15px;
    }
    header nav ul {
        margin-top: 10px;
    }
    header > div > div { /* Search and Cart container */
        flex-direction: column; /* Stack search input and buttons */
        width: 100%;
        gap: 10px;
    }
    header input[type="text"] {
        width: 90%; /* Wider search input */
    }

    #HomePage > div:first-of-type {
        padding: 15px; /* Adjust padding */
    }

    #HomePage button {
        width: 80%; /* Wider buttons */
    }

    #productDetailsModal > div {
        flex-direction: column; /* Stack image and text vertically in modal */
        width: 95%; /* Wider on small screens */
        padding: 20px;
    }
    #productDetailsModal img {
        max-width: 100%; /* Ensure image uses full available width */
    }

    #productDetailsModal > div > div:last-of-type {
        padding-top: 15px; /* Add some space above text content */
    }

    #cartModal > div {
        width: 100%; /* Cart takes full width on small screens */
        padding: 15px;
    }
    #cartModal h3 {
        font-size: 22px;
    }
    #cartModal button:first-of-type {
        font-size: 24px;
        top: 10px;
        right: 15px;
    }

    #cartItemsContainer > div {
        flex-direction: column; /* Stack cart item elements if needed */
        align-items: flex-start;
        gap: 8px;
    }
    #cartItemsContainer img {
        margin-right: 0;
        margin-bottom: 5px;
    }
    #cartItemsContainer input[type="number"] {
        margin-left: 0; /* Align quantity input */
        width: 80px;
    }
    #cartItemsContainer button {
        margin-top: 5px;
    }

    #checkoutPage form {
        grid-template-columns: 1fr; /* Single column for checkout form */
    }
    #checkoutPage > div {
        padding: 20px;
    }
}
